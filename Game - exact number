Напиши ми програма, игра. Която да познава точно число от 1 до 100, до три опита.
+При всеки опит да ти казва дали числото, което си въвел е по-голямо или по-малко от това, което трябва.
+При познаване на числото да се изписва "Браво! Познахте числото за X опита."
+При неуспех да се изписва "Неуспех! Числото е: Y."
+*/
+
+#include <iostream>
+#include <cstdlib>
+#include <ctime>
+using namespace std;
+
+int main() {
+    int number, guess, tries = 0;
+    srand(time(0)); // започваме генерирането на случайни числа
+    number = rand() % 100 + 1; // генерираме число от 1 до 100
+    do {
+        cout << "Въведете число: ";
+        cin >> guess;
+        if (guess > number) {
+            cout << "Числото е по-малко.\n";
+        } else if (guess < number) {
+            cout << "Числото е по-голямо.\n";
+        }
+        tries++;
+    } while (guess != number && tries < 3);
+    if (guess == number) {
+        cout << "Браво! Познахте числото за " << tries << " опита.\n";
+    } else {
+        cout << "Неуспех! Числото е: " << number << ".\n";
+    }
+    return 0;
+}
